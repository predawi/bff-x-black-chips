/**
 * Init stuffs
 */

document.addEventListener('DOMContentLoaded', () => {
  // Click outside
  /* document.addEventListener('click', event => {
    const el = document.querySelector('#el').contains(event.target)
    const elContainer = document.querySelector('#elContainer').contains(event.target)
    if (!el && !elContainer) {
      document.body.classList.remove('example')
    }
  }) */
})
